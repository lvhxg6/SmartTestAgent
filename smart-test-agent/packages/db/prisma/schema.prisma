// Prisma schema placeholder
// Full schema will be defined in task 1.3

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
